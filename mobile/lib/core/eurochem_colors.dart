import 'package:flutter/material.dart';

/// Корпоративные цвета ЕвроХим из брендбука v1.3
class EurochemColors {
  // Основные цвета
  static const Color primaryBlue = Color(0xFF053868);      // Pantone 2955 C - основной синий
  static const Color lightBlue = Color(0xFF00B0F0);        // Pantone 2995 C - голубой (акценты)
  static const Color green = Color(0xFF95C11F);            // Pantone 376C - зелёный (успех, безопасность)
  static const Color yellow = Color(0xFFFFC211);           // Pantone 1235C - жёлтый (предупреждения)
  static const Color gray = Color(0xFF6D727A);             // Pantone 431 - серый (второстепенное)
  
  // Дополнительные оттенки
  static const Color darkBlue = Color(0xFF001529);         // Для сайдбара (как в веб-версии)
  static const Color lightGreen = Color(0xFFE6F7EE);       // Для выделения элементов
}